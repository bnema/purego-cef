// Code generated by cefgen. DO NOT EDIT.

package cef

import (
	"unsafe"

	"github.com/ebitengine/purego"

	"github.com/bnema/purego-cef/cef/internal/raw"
)

// App Implement this structure to provide handler implementations. Methods will be called by the process and/or thread indicated.
type App interface {
	// OnBeforeCommandLineProcessing Provides an opportunity to view and/or modify command-line arguments before processing by CEF and Chromium. The |process_type| value will be NULL for the browser process. Do not keep a reference to the cef_command_line_t object passed to this function. The cef_settings_t.command_line_args_disabled value can be used to start with an NULL command-line object. Any values specified in CefSettings that equate to command-line arguments will be set before this function is called. Be cautious when using this function to modify command-line arguments for non-browser processes as this may result in undefined behavior including crashes.
	OnBeforeCommandLineProcessing(processType string, commandLine CommandLine)
	// OnRegisterCustomSchemes Provides an opportunity to register custom schemes. Do not keep a reference to the |registrar| object. This function is called on the main thread for each process and the registered schemes should be the same across all processes.
	OnRegisterCustomSchemes(registrar SchemeRegistrar)
	// GetResourceBundleHandler Return the handler for resource bundle events. If no handler is returned resources will be loaded from pack files. This function is called by the browser and render processes on multiple threads.
	GetResourceBundleHandler() ResourceBundleHandler
	// GetBrowserProcessHandler Return the handler for functionality specific to the browser process. This function is called on multiple threads in the browser process.
	GetBrowserProcessHandler() BrowserProcessHandler
	// GetRenderProcessHandler Return the handler for functionality specific to the render process. This function is called on the render process main thread.
	GetRenderProcessHandler() RenderProcessHandler
}

// NewApp creates a CEF handler backed by the given implementation.
// The returned pointer can be passed to CEF functions that expect this handler type.
func NewApp(impl App) unsafe.Pointer {
	r := new(raw.CEFAppT)
	initRefCount(unsafe.Pointer(r), unsafe.Sizeof(*r), r)

	r.OverrideOnBeforeCommandLineProcessing(purego.NewCallback(func(_ uintptr, _ uintptr) {
		// TODO: unmarshal args, call impl.OnBeforeCommandLineProcessing(...)
	}))

	r.OverrideOnRegisterCustomSchemes(purego.NewCallback(func(_ uintptr) {
		// TODO: unmarshal args, call impl.OnRegisterCustomSchemes(...)
	}))

	// Cache the getter result once.
	cachedGetResourceBundleHandler := impl.GetResourceBundleHandler()
	r.OverrideGetResourceBundleHandler(purego.NewCallback(func(_ uintptr) uintptr {
		_ = cachedGetResourceBundleHandler
		return 0 // TODO: marshal cached result
	}))

	// Cache the getter result once.
	cachedGetBrowserProcessHandler := impl.GetBrowserProcessHandler()
	r.OverrideGetBrowserProcessHandler(purego.NewCallback(func(_ uintptr) uintptr {
		_ = cachedGetBrowserProcessHandler
		return 0 // TODO: marshal cached result
	}))

	// Cache the getter result once.
	cachedGetRenderProcessHandler := impl.GetRenderProcessHandler()
	r.OverrideGetRenderProcessHandler(purego.NewCallback(func(_ uintptr) uintptr {
		_ = cachedGetRenderProcessHandler
		return 0 // TODO: marshal cached result
	}))

	return unsafe.Pointer(r)
}

// GetExitCode This function can optionally be called on the main application thread after CefInitialize to retrieve the initialization exit code. When CefInitialize returns true (1) the exit code will be 0 (CEF_RESULT_CODE_NORMAL_EXIT). Otherwise, see cef_resultcode_t for possible exit code values including browser process initialization errors and normal early exit conditions (such as CEF_RESULT_CODE_NORMAL_EXIT_PROCESS_NOTIFIED for process singleton relaunch behavior).
func GetExitCode() int32 {
	// TODO: marshal args, call raw.CEFGetExitCode(...), marshal return
	_ = raw.CEFGetExitCode
	return 0
}

// RunMessageLoop wraps the CEF CEFRunMessageLoop function.
func RunMessageLoop() {
	// TODO: marshal args and call raw.CEFRunMessageLoop(...)
	_ = raw.CEFRunMessageLoop
}

// QuitMessageLoop Quit the CEF message loop that was started by calling cef_run_message_loop(). This function should only be called on the main application thread and only if cef_run_message_loop() was used.
func QuitMessageLoop() {
	// TODO: marshal args and call raw.CEFQuitMessageLoop(...)
	_ = raw.CEFQuitMessageLoop
}

// SetNestableTasksAllowed Set to true (1) before calling OS APIs on the CEF UI thread that will enter a native message loop (see usage restrictions below). Set to false (0) after exiting the native message loop. On Windows, use the CefSetOSModalLoop function instead in cases like native top menus where resize of the browser content is not required, or in cases like printer APIs where reentrancy safety cannot be guaranteed. Nested processing of Chromium tasks is disabled by default because common controls and/or printer functions may use nested native message loops that lead to unplanned reentrancy. This function re-enables nested processing in the scope of an upcoming native message loop. It must only be used in cases where the stack is reentrancy safe and processing nestable tasks is explicitly safe. Do not use in cases (like the printer example) where an OS API may experience unplanned reentrancy as a result of a new task executing immediately. For instance, - The UI thread is running a message loop. - It receives a task #1 and executes it. - The task #1 implicitly starts a nested message loop. For example, via   Windows APIs such as MessageBox or GetSaveFileName, or default handling of   a user-initiated drag/resize operation (e.g. DefWindowProc handling of   WM_SYSCOMMAND for SC_MOVE/SC_SIZE). - The UI thread receives a task #2 before or while in this second message   loop. - With NestableTasksAllowed set to true (1), the task #2 will run right   away. Otherwise, it will be executed right after task #1 completes at   "thread message loop level".
func SetNestableTasksAllowed(allowed int32) {
	// TODO: marshal args and call raw.CEFSetNestableTasksAllowed(...)
	_ = raw.CEFSetNestableTasksAllowed
}
