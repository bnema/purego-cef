// Code generated by cefgen. DO NOT EDIT.

package cef

import (
	"unsafe"

	"github.com/ebitengine/purego"

	"github.com/bnema/purego-cef/cef/internal/raw"
)

// AudioHandler Implement this structure to handle audio events.
type AudioHandler interface {
	// GetAudioParameters Called on the UI thread to allow configuration of audio stream parameters. Return true (1) to proceed with audio stream capture, or false (0) to cancel it. All members of |params| can optionally be configured here, but they are also pre-filled with some sensible defaults.
	GetAudioParameters(browser Browser, params uintptr) int32
	// OnAudioStreamStarted Called on a browser audio capture thread when the browser starts streaming audio. OnAudioStreamStopped will always be called after OnAudioStreamStarted; both functions may be called multiple times for the same browser. |params| contains the audio parameters like sample rate and channel layout. |channels| is the number of channels.
	OnAudioStreamStarted(browser Browser, params uintptr, channels int32)
	// OnAudioStreamPacket Called on the audio stream thread when a PCM packet is received for the stream. |data| is an array representing the raw PCM data as a floating point type, i.e. 4-byte value(s). |frames| is the number of frames in the PCM packet. |pts| is the presentation timestamp (in milliseconds since the Unix Epoch) and represents the time at which the decompressed packet should be presented to the user. Based on |frames| and the |channel_layout| value passed to OnAudioStreamStarted you can calculate the size of the |data| array in bytes.
	OnAudioStreamPacket(browser Browser, data uintptr, frames int32, pts int64)
	// OnAudioStreamStopped Called on the UI thread when the stream has stopped. OnAudioSteamStopped will always be called after OnAudioStreamStarted; both functions may be called multiple times for the same stream.
	OnAudioStreamStopped(browser Browser)
	// OnAudioStreamError Called on the UI or audio stream thread when an error occurred. During the stream creation phase this callback will be called on the UI thread while in the capturing phase it will be called on the audio stream thread. The stream will be stopped immediately.
	OnAudioStreamError(browser Browser, message string)
}

// NewAudioHandler creates a CEF handler backed by the given implementation.
// The returned pointer can be passed to CEF functions that expect this handler type.
func NewAudioHandler(impl AudioHandler) unsafe.Pointer {
	r := new(raw.CEFAudioHandlerT)
	initRefCount(unsafe.Pointer(r), unsafe.Sizeof(*r), r)

	r.OverrideGetAudioParameters(purego.NewCallback(func(_ uintptr, _ uintptr) uintptr {
		// TODO: unmarshal args, call impl.GetAudioParameters(...), marshal return
		return 0
	}))

	r.OverrideOnAudioStreamStarted(purego.NewCallback(func(_ uintptr, _ uintptr, _ uintptr) {
		// TODO: unmarshal args, call impl.OnAudioStreamStarted(...)
	}))

	r.OverrideOnAudioStreamPacket(purego.NewCallback(func(_ uintptr, _ uintptr, _ uintptr, _ uintptr) {
		// TODO: unmarshal args, call impl.OnAudioStreamPacket(...)
	}))

	r.OverrideOnAudioStreamStopped(purego.NewCallback(func(_ uintptr) {
		// TODO: unmarshal args, call impl.OnAudioStreamStopped(...)
	}))

	r.OverrideOnAudioStreamError(purego.NewCallback(func(_ uintptr, _ uintptr) {
		// TODO: unmarshal args, call impl.OnAudioStreamError(...)
	}))

	return unsafe.Pointer(r)
}
